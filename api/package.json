{
  "name": "tsoa-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsoa": "tsoa",
    "tsoa:routes": "tsoa routes",
    "tsoa:spec": "tsoa spec",
    "generate": "npm run tsoa:routes && npm run tsoa:spec",
    "start": "node dist/app.js",
    "tsup": "tsup",
    "build": "tsup src/app.ts --legacy-output --format cjs,esm --dts",
    "dev": "tsup --config ./tsup.config.ts --watch src/ --onSuccess \"node dist/app.js\"",
    "swagger": "tsoa spec",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migration:run": "npx sequelize-cli db:migrate",
    "migration:generate": "npx sequelize-cli migration:generate",
    "migration:revert": "npx sequelize-cli db:migrate:undo",
    "migration:create": "npx sequelize-cli migration:generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tsoa/runtime": "^6.4.0",
    "dotenv": "^16.4.5",
    "express": "5.0.0",
    "morgan": "^1.10.0",
    "pg": "^8.13.0",
    "sequelize": "^6.37.4",
    "sequelize-cli": "^6.6.2",
    "swagger-ui-express": "^5.0.1",
    "tsoa": "^6.4.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/morgan": "^1.9.9",
    "@types/node": "^22.5.5",
    "@types/swagger-ui-express": "^4.1.6",
    "tsup": "^8.3.0"
  }
}

